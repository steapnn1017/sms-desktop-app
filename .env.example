# Database URL for Prisma (SQLite)
# In production, this is set dynamically by Electron to the user's AppData folder
# For local development / Prisma CLI:
DATABASE_URL="file:./prisma/dev.db"

# Node environment
NODE_ENV=development
